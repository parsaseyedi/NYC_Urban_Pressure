# Data

Large raw data files are not stored directly in this GitHub repository; The raw data snapshot used for this project is provided separately as a GitHub Release asset named data.zip.

After downloading data.zip, extract it into the project root so that you have them as:
NYC_Urban_Pressure\data directory

